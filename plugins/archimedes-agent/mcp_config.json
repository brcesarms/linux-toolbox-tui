{
  "mcpServers": {
    "archimedes-rag": {
      "serverUrl": "http://127.0.0.1:8765/mcp",
      "description": "Busca semântica local (AST + LanceDB) do Archimedes V2 — índice local via streamable-http (porta 8765)."
    }
  }
}
